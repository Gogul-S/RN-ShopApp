class CartItem {
    constructor(productTitle,sum,productPrice,quantity) {
        this.productTitle = productTitle;
        this.sum = sum;
        this.productPrice = productPrice;
        this.quantity = quantity;
    }
}

export default CartItem;